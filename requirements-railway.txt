# Requirements for Railway deployment (with semantic search)
# Core FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# Database (supabase gère ses propres dépendances httpx)
supabase>=2.3.0

# Data Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Scheduler (for cron jobs)
apscheduler>=3.10.4
pytz>=2024.1

# Data Processing
xmltodict>=0.13.0
numpy>=1.24.0

# Semantic Search (CPU-only, lighter weight)
# Use --index-url for CPU-only torch to save ~500MB
sentence-transformers>=2.2.0

# Note: torch will be installed by sentence-transformers
# Railway memory limit may require the smaller model
# Consider using "all-MiniLM-L6-v2" instead of "multilingual-e5-large" in production
