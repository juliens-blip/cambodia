# Requirements for Railway deployment (full features)
# Core FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# Database (supabase gère ses propres dépendances httpx)
supabase>=2.3.0

# Data Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Scheduler (for cron jobs)
apscheduler>=3.10.4
pytz>=2024.1

# Data Processing
xmltodict>=0.13.0
numpy>=1.24.0

# Semantic Search (for local document analysis)
sentence-transformers>=2.2.0
torch>=2.0.0

# Note: This adds ~500MB to the container but enables:
# - Semantic search in local PDFs
# - Document-based RAG analysis
# - Multilingual embeddings (Khmer, English, Vietnamese)
