# Dockerfile pour l'UI Streamlit
# Service frontend

FROM python:3.11-slim-bookworm

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PORT=8501

# Installer les dépendances système
RUN apt-get update && apt-get install -y --no-install-recommends \
    poppler-utils \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Copier les requirements Streamlit
COPY requirements-streamlit.txt ./

# Installer les dépendances
RUN pip install --no-cache-dir -r requirements-streamlit.txt

# Copier le code
COPY app/ ./app/
COPY ui/ ./ui/

# Exposer le port
EXPOSE 8501

# Démarrage Streamlit
CMD ["python", "-c", "import os; port = os.environ.get('PORT', '8501'); import subprocess; subprocess.run(['streamlit', 'run', 'ui/streamlit_app.py', '--server.port', port, '--server.address', '0.0.0.0', '--server.headless', 'true'])"]
